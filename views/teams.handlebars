<div class="container demo-1">
			<!-- Top Navigation -->

      <br>
      <br>
      <br>
      <div class="text-center">
      <h3><i class="fas fa-users"></i> Teams</h3>
      </div>
      
      <button type="button" class="btn btn-block btn-dark" data-toggle="modal" data-target="#startDanceoff"><i class="fas fa-robot"></i> Let's get this road on the show</button>
 <div class="col-xs-5">
<h2 class="text-uppercase sub-header">Team {{teamOne.teamName}}</h2>
<h3 class="sub-header">Experience {{teamOne.experience}}</h3>
  <div class="table-responsive">
                
                 <table id="searchTable" class="table table-dark table-hover" >
  
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">Robot Name</th>
                        <th scope="col">Power Move</th>
                        <th scope="col">Experience</th>
                        <th scope="col">Avatar</th>
                        
                        

                    </tr>
                </thead>
                       
            
        {{#each teamOne}}
        



  
   </script>
       <tbody>
                    
                        <tr>
                            <td>{{this.name}}</td>
                            <td>{{this.powermove}}</td>
                            <td>{{this.experience}}</td> 
                            <td> <button class="btn btn-info" type="button" data-toggle="modal" data-target="#info{{this.id}}">
              
              <span class="pl-1 d-none d-sm-inline-block">View</span>
            </button></td>
                            
                        </tr>
                    
                </tbody>
       
          






                     <!--Avatar Modal-->
            <div class="modal fade" id="info{{this.id}}" tabindex="-1" role="dialog" aria-labelledby="More Info Modal"
              aria-hidden="true">
              <div class="modal-dialog modal-dialog-centered" role="document">
                <div class="modal-content">
                  <div class="modal-header">
                    <h5 class="modal-title text-secondary font-weight-bold text-center"><i
                        class=""></i> {{this.name}}</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                      <span aria-hidden="true">&times;</span>
                    </button>
                  </div>
                  <div class="modal-body text-secondary">
                    <div class="d-flex justify-content-center">
                    <img class="rounded-circle img-fluid" src="{{this.avatar}}" alt="{{this.name}}">
                    </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Ok, Got it</button>
                    
                  </div>
                </div>
              </div>
            </div>
          </div>
        {{/each}}
          </table>
          </div>

          <div class="col-xs-5">
<h2 class="text-uppercase sub-header">Team {{teamTwo.teamName}}</h2>
<h3 class="sub-header">Experience {{teamTwo.experience}}</h3>
  <div class="table-responsive">
                 <table id="searchTable" class="table table-dark table-hover" >
  
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">Robot Name</th>
                        <th scope="col">Power Move</th>
                        <th scope="col">Experience</th>
                        <th scope="col">Avatar</th>
                        
                        

                    </tr>
                </thead>
                       
            
        {{#each teamTwo}}
        



  
   </script>
       <tbody>
                    
                        <tr>
                            <td>{{this.name}}</td>
                            <td>{{this.powermove}}</td>
                            <td>{{this.experience}}</td> 
                            <td> <button class="btn btn-info" type="button" data-toggle="modal" data-target="#info{{this.id}}">
              
              <span class="pl-1 d-none d-sm-inline-block">View</span>
            </button></td>
                            
                        </tr>
                    
                </tbody>
       
          






                     <!--Avatar Modal-->
            <div class="modal fade" id="info{{this.id}}" tabindex="-1" role="dialog" aria-labelledby="More Info Modal"
              aria-hidden="true">
              <div class="modal-dialog modal-dialog-centered" role="document">
                <div class="modal-content">
                  <div class="modal-header">
                    <h5 class="modal-title text-secondary font-weight-bold"> {{this.name}}</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                      <span aria-hidden="true">&times;</span>
                    </button>
                  </div>
                  <div class="modal-body text-secondary">
                    <div class="d-flex justify-content-center">
                    <img class="rounded-circle img-fluid" src="{{this.avatar}}" alt="{{this.name}}">
                    </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Ok, Got it</button>
                    
                  </div>
                </div>
              </div>
            </div>
          </div>
        {{/each}}
          </table>
          </div>

          <div class="modal fade" id="startDanceoff" tabindex="-1" role="dialog" aria-labelledby="Search Modal"
              aria-hidden="true">
              <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                <div class="modal-content">
                  <div class="modal-header">
                    <h5 class="modal-title text-secondary font-weight-bold"><i class="fas fa-robot"></i> How many danceoffs would you like</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                      <span aria-hidden="true">&times;</span>
                    </button>
                  </div>
                  <div class="modal-body text-secondary">
          	<form action="danceoff" method="POST">
               <input type="range" min="1" max="5" value="5" class="slider" id="danceoffs" name="danceoffs" disabled>
                 <p>Danceoffs: <span id="danceoffAmount"></span></p>
                 <input type="hidden" name="danceoffId" value="{{danceoffCompetetors}}">
                 <button class="btn btn-primary" type="submit">Let's get ready to dance</button>
            </form>

            <script>
             var slider = document.getElementById("danceoffs");
var output = document.getElementById("danceoffAmount");
output.innerHTML = slider.value; // Display the default slider value

// Update the current slider value (each time you drag the slider handle)
slider.oninput = function() {
  output.innerHTML = this.value;
}
            </script>
		</div><!-- /container -->
		
                  <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">No, i'll go back</button>
                    
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!--Enter Team Name-->
            <div class="modal fade" id="enterTeam" tabindex="-1" role="dialog" aria-labelledby="Search Modal"
              aria-hidden="true">
              <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                <div class="modal-content">
                  <div class="modal-header">
                    <h5 class="modal-title text-secondary font-weight-bold"><i class="fas fa-robot"></i> Give your Teams Names</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                      <span aria-hidden="true">&times;</span>
                    </button>
                  </div>
                  <div class="modal-body text-secondary">
          		<div class="main clearfix">
					  <h3><code>Click on the <u>underlined</u> sections to enter a Team Name</code></h3>
				<form id="nl-form" class="nl-form" action="teams" method="GET">
					In the left corner we have  
					<input type="text" name="teamOne" value="" placeholder="the Home Team" data-subline="For example: <em>The Rust Buckets</em> or <em>The Tin Cans</em>" required/>
					<br />and in the right corner we have
					<input type="text" name="teamTwo" value="" placeholder="the other Team" data-subline="For example: <em>Circuit Breakers</em> or <em>Nuts and Bolts</em>" required/>
					
					<div class="nl-submit-wrap">
						<button class="nl-submit" type="submit">Get the Team Members</button>
						
					</div>
					<div class="nl-overlay"></div>

				
				</form>
<br>
<br>
				
					
			</div>
		</div><!-- /container -->
		<script src="javascripts/nlform.js"></script>
		<script>
			var nlform = new NLForm( document.getElementById( 'nl-form' ) );
		</script>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">No, i'll go back</button>
                    
                  </div>
                </div>
              </div>
            </div>
          </div>
	</body>